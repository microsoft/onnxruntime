Build Output
The build will generate output in $REPO_ROOT/build/$OS/$CONFIGURATION/java/build:

docs/javadoc/ - HTML javadoc
reports/ - detailed test results and other reports
libs/onnxruntime-VERSION.jar - JAR with compiled classes, platform-specific JNI shared library,
and platform-specific onnxruntime shared library
