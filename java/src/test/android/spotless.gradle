spotless {
    format 'misc', {
        // define the files to apply `misc` to
        target 'build.gradle',

        // define the steps to apply to files
        indentWithSpaces()
        trimTrailingWhitespace()
        endWithNewline()
    }

    java  {
        target 'src/androidTest/java/ai/onnxruntime/example/javavalidator/SimpleTest.java'
    }
}