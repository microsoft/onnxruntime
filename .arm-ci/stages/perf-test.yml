# #
# # SPDX-FileCopyrightText: Copyright 2025 Arm Limited and/or its affiliates <open-source-office@arm.com>
# #
# # SPDX-License-Identifier: MIT
# #
# perf_test_models:
#   extends:
#     - .mac_common
#   stage: perf_test
#   tags:
#     - mac-mini
#   needs:
#     - job: mac_release_build
#       artifacts: true
#   script:
#      - |

#         # Move to the correct directory
#         cd build/MacOS/Release/Release
#         # Load or clone model sources here
#         # e.g. via git clone to somewhere like ~/models
#         # Or download from license-controlled source (SS, vivo, etc.)


#         # Iterate through all .onnx models in the directory
#         for model in *.onnx; do
#           profile_name=$(basename "$model" .onnx)
#           echo "Running performance test for $model with profile name $profile_name"
#           ./onnxruntime_perf_test -v -e cpu -I -m times -r 50 -p "$profile_name" "$model"
#         done  allow_failure: true
#   artifacts:
#     paths:
#      - build/MacOS/Release/Release/*.json
#     expire_in: 7d
