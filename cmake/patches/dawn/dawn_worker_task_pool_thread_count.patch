diff --git a/include/dawn/platform/DawnPlatform.h b/include/dawn/platform/DawnPlatform.h
--- a/include/dawn/platform/DawnPlatform.h
+++ b/include/dawn/platform/DawnPlatform.h
@@ -204,6 +204,9 @@ class DAWN_PLATFORM_EXPORT Platform {
     // stuck.
     virtual void ReportProgress();
 
+  protected:
+    std::unique_ptr<WorkerTaskPool> CreateDefaultWorkerTaskPool(uint32_t maxThreadCount);
+
   private:
     Platform(const Platform&) = delete;
     Platform& operator=(const Platform&) = delete;
diff --git a/src/dawn/platform/DawnPlatform.cpp b/src/dawn/platform/DawnPlatform.cpp
--- a/src/dawn/platform/DawnPlatform.cpp
+++ b/src/dawn/platform/DawnPlatform.cpp
@@ -91,6 +91,11 @@ std::unique_ptr<dawn::platform::WorkerTaskPool> Platform::CreateWorkerTaskPool() {
     return std::make_unique<AsyncWorkerThreadPool>();
 }
 
+std::unique_ptr<dawn::platform::WorkerTaskPool> Platform::CreateDefaultWorkerTaskPool(
+    uint32_t maxThreadCount) {
+    return std::make_unique<AsyncWorkerThreadPool>(maxThreadCount);
+}
+
 std::unique_ptr<dawn::platform::JobHandle> WorkerTaskPool::PostWorkerJob(PostWorkerJobCallback cb,
                                                                         void* userdata) {
     DAWN_UNREACHABLE();
