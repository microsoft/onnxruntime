--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -113,6 +113,12 @@
 message(STATUS "PAL implementation: ${PAL_IMPLEMENTATION}")
 string(TOUPPER ${PAL_IMPLEMENTATION} PAL_IMPLEMENTATION_UPPER)
 add_library(matsdk_internal_config INTERFACE)
+option(MATSDK_DISABLE_LOGGING "Compile internal SDK logging out" OFF)
+if(MATSDK_DISABLE_LOGGING)
+  target_compile_definitions(matsdk_internal_config INTERFACE
+    MATSDK_DISABLE_LOGGING)
+endif()
+
 target_compile_definitions(matsdk_internal_config INTERFACE
   MATSDK_PAL_${PAL_IMPLEMENTATION_UPPER}=1
   NOMINMAX)

--- a/lib/include/mat/config-default.h
+++ b/lib/include/mat/config-default.h
@@ -28,7 +28,9 @@
 #endif
 #define HAVE_MAT_JSONHPP
 #define HAVE_MAT_ZLIB
+#if !defined(MATSDK_DISABLE_LOGGING)
 #define HAVE_MAT_LOGGING
+#endif
 /* #define HAVE_MAT_WIN_LOG     */
 /* #define HAVE_MAT_EVT_TRACEID     */
 #define HAVE_MAT_STORAGE

--- a/lib/pal/PAL.cpp
+++ b/lib/pal/PAL.cpp
@@ -185,4 +185,5 @@
 #if !defined(_WIN32) && defined(__linux__)
+#ifdef HAVE_MAT_LOGGING
         static std::mutex m;
         static std::map<std::thread::id, pid_t> threads;
         static long int gettid()
@@ -192,6 +193,7 @@
             threads[std::this_thread::get_id()] = tid;
             return tid;
         }
+#endif
 #else
 #define     gettid()       std::this_thread::get_id()
 #endif

--- a/lib/pal/TaskDispatcher_CAPI.cpp
+++ b/lib/pal/TaskDispatcher_CAPI.cpp
@@ -46,4 +46,5 @@
                 }
                 catch (const std::exception& ex) {
+                    (void)ex;
                     LOG_ERROR("Unhandled exception in CAPI task: %s", ex.what());
                 }

--- a/lib/pal/WorkerThread.cpp
+++ b/lib/pal/WorkerThread.cpp
@@ -249,4 +249,5 @@
                         }
                         catch (const std::exception& ex) {
+                            (void)ex;
                             LOG_ERROR("Unhandled exception in worker task: %s", ex.what());
                         }
