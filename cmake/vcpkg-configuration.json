{
    "$comment": "Baseline bumped to pick up the cpp-client-telemetry port (POSIX 1DS telemetry), which did not exist in the prior baseline. ONNX Runtime's heavy dependencies are pinned via overlay-ports/overrides (see cmake/vcpkg.json), so this bump only floats benign minor versions; impact was verified with 'vcpkg install --dry-run'.",
    "default-registry": {
        "kind": "git",
        "repository": "https://github.com/Microsoft/vcpkg",
        "baseline": "22b8d099947ea6ee2fcb1aa1124b21f48f84232d"
    },
    "overlay-ports": [
        "./vcpkg-ports"
    ],
    "registries": []
}
